import 'package:flutter/material.dart';

class QuectionScreen extends StatelessWidget {
  const QuectionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Quection Screen")));
  }
}
